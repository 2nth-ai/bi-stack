# Extra Python deps baked into the Superset image.
# Pinned to stable versions that match Superset 4.1.x wheels.
psycopg2-binary==2.9.9
redis==5.0.7
flask-caching==2.3.0
flask-cors==4.0.1
# FAB 4.5.4+ stringifies JWT subject — required with pyjwt>=2.10.1 which
# rejects int subjects. Without this, Superset API auth 422s.
flask-appbuilder>=4.5.4,<4.6
pyjwt>=2.10.1,<3.0
